{%- comment -%}
  Icon Object
    Object Tool for importing an icon

  Parameters:
    icon - The icon to use
    class - Extra (optional) classes you want to add to the icon
    title - Reader friendly title and alt tag
    alt - (Optional) override for the alt tag.
    attributes - (Optional) Attributes to apply to the SVG/Image
    width - The width dimensions to apply to the image tag
    height - The height dimensions to apply to the image tag

  Version:
    2.0.0 - 2020/08/24
{%- endcomment -%}
{%- liquid
  capture classes
    echo 'o-icon o-icon--'
    echo icon | handlize
    if class != blank
      echo ' ' | append: class
    endif
  endcapture

  capture filename
    echo 'icon-'
    echo icon | handlize
    echo '.svg'
  endcapture
-%}

<img
  src="{{ filename | asset_url }}"
  alt="{{ alt | escape }}"
  {% if attributes %}{{ attributes }}{% endif %}
  width="{{ width | default: 'auto' }}"
  height="{{ height | default: 'auto' }}"
  class="{{ classes }}"
/>
